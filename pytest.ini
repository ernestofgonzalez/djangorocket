[pytest]
testpaths = tests
python_files = tests.py test_*.py
addopts = -p no:warnings --strict-markers
markers =
    e2e: end-to-end tests that bake the template and boot the generated project (slow; builds a venv). Enable with --run-e2e.
